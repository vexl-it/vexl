# AGENTS

Purpose: Shared UI package providing the Vexl design system built on Tamagui. Native-only (no web/media queries).

Stack: TypeScript, Tamagui v1, React, react-native-reanimated v4.

Structure:

- `src/config/` — Tamagui configuration (tokens, themes, fonts).
- `src/provider/` — VexlThemeProvider and theme hooks.
- `src/primitives/` — Re-exported Tamagui primitive components (Stack, Text, etc.).
- `src/assets/fonts/` — Bundled font files (TT Satoshi 400/500/600/700 `.otf`, PP Monument 400/700 `.ttf`). Exported as `vexlFonts` record for direct use with `useFonts()` from expo-font. Font face names (`TTSatoshi{400,500,600,700}`, `PPMonument{400,700}`) match the `face` mapping in `src/config/fonts.ts`.
- `src/assets/anonymousAvatars/` — Auto-generated anonymous avatar SVG components (31 basic + 27 golden). Each accepts `size` and `grayscale` props. Grayscale uses SVG `<Filter>`/`<FeColorMatrix type="saturate" values="0">`. Generated by `scripts/convert-avatars.mjs`. Exported as `avatarsSvg` and `avatarsGoldenGlassesAndBackgroundSvg`. Files use `// prettier-ignore` on the return statement to prevent Prettier from inserting `{' '}` text nodes (which crash in react-native-svg).
- `src/components/` — Custom Vexl design-system components. See inventory below; read the component file directly for props, exact tokens, and layout details.

## Component inventory

Read the component source in `src/components/<Name>/` for full props, tokens, and layout.

Cards & containers:

- `Banner` — Promo/info banner card (green/pink), optional image, up to 2 inline buttons.
- `BlogCard` — Image-over-content article card with asymmetric radii.
- `ClubCard` — Club membership card with avatar, name, subtitle, optional tag.
- `CommonFriends` — Mutual friends card with clipped chip row + gradient fade.
- `EventCard` — Two-part event card (header/content) with upcoming/past states and attendee chips.
- `Note` — Pin/note card with avatar, metadata, message.
- `NotificationCard` — Notification card with header (avatar/name/time) and content.
- `OfferCard` — Offer/request card with tags, price, description, details row.
- `Menu` + `MenuItem` — Grouped menu container with auto-inserted dividers (default/danger variants).
- `ReachStats` — Reach/deals stats card with horizontal progress stepper + action button.
- `Screen` — Full-screen wrapper (navigationBar / scroll content / footer).

Buttons:

- `Button` — Primary action button (5 variants × 3 sizes).
- `CardButton` — Small card-level button (filled/text × contrast flag).
- `FabButton` — Floating action button (icon + label, yellow primary).
- `IconButton` — Square icon button with optional `Dot` badge.
- `NavButton` — Nav-bar button (icon/text × highlighted/destructive/normal).
- `RowButton` — Full-width row button (default/selected states).

Inputs & selection:

- `SearchBar` — Pill-shaped search input with jotai `valueAtom`.
- `TextField` — Text input with 5 right-element modes (none, clear, checkmark, button, icon).
- `Exchange` — Dual BTC/SATS + fiat input with centered arrows badge.
- `Picker` — Custom dropdown picker with tap-to-toggle item list (Tamagui Select is unreliable on RN).
- `RadioGroup` + `RowRadiobutton` — Typed radio group wrapper + full-width radio row.
- `RowCheckbox` — Full-width checkbox row (default/checked states).
- `Selector` — Full-width row selector (action → chevron, switch → toggle).
- `SelectableItem` — Selectable list item with radio icon.
- `SelectClubCell` — Club multi-select cell (avatar ↔ checkbox icon).
- `SegmentedPicker` — Generic segmented toggle picker.
- `RangeSlider` — Dual-thumb logarithmic range slider.
- `RadiusSlider` — Single-thumb range slider for map radius.
- `BuySellRangeSlider` — Single-thumb percentage slider with distance-based info banner.
- `PriceRangeInput` — Composite min/max inputs + currency button + `RangeSlider`.
- `InputHint` — Hint/error text row below inputs.

Navigation & tabs:

- `NavigationBar` — Header bar with 3 styles (main, back, chat) and `scrolled` bg toggle.
- `TabBar` — Bottom tab bar for main screens (generic over `T`, optional badges).
- `Tabs` — Horizontal tab bar with animated underline (large/small).
- `FilterBar` + `FilterTag` — Horizontally scrollable filter tags (generic, multi-select).

Lists & cells:

- `ChatMessageItem` — Chat list row (4 color variants, unread dot, typing indicator).
- `ChecklistCell` — Checklist row (initial, pending, completed states).
- `EditRow` — Offer edit row (initial, editing, completed, profile states).
- `StepperCheck` — Vertical stepper item with connector line.

Tags & indicators:

- `Chip` — Small avatar + name chip used by `CommonFriends`/`EventCard`.
- `TextTag` — Status tag with 10 typed variants.
- `IconTag` — Icon-only tag with 3 typed variants (bitcoin/product/service).
- `Dot` — Small/number circle indicator.
- `DotTypingIndicator` — 3-dot bouncing typing indicator.

Feedback & overlays:

- `Dialog` — Jotai-driven bottom-sheet dialog. Create with `createDialogAtom()` → `DialogAtom`. Show via `set(dialogAtom, config)` which returns `Effect.Effect<boolean>` (true = positive, false = negative/dismiss). Render `<DialogFromAtom dialogAtom={...} />` once in the tree.
- `Toast` — Jotai-driven top toast (3s auto-dismiss, fade in/out).
- `Loader` — RN `ActivityIndicator` wrapper (small/medium/large).

Other:

- `Avatar` — Rectangular avatar with grayscale support via `FilterImage` (raster) or child component.
- `FriendLevel` — Connection-degree selection card (1st/2nd degree).

## Design tokens

Sourced from Figma file `P7IaNcwu4qoS9uTL7ECiWL` (Vexl redesign DEV, node 593:39715):

- **Colors**: 24 semantic theme tokens (light/dark) across accents, background, foreground, vibrant groups.
- **Spacing**: 14-step scale (0px–80px).
- **Corner radius**: 13-step scale (0px–40px).
- **Typography**: TT Satoshi (body, weights 500/600) and Monument Extended (heading, weights 400/700).

## Component patterns

- Purely visual components use `styled()` with a `name` property matching the export name.
- Components needing coordinated styling (e.g. frame + label sharing a variant) use a functional component that composes internal styled primitives and forwards variant props to each.
- Use theme tokens (`$foregroundPrimary`) over raw color tokens (`$black100`) unless the design intentionally avoids theme-switching (e.g. CardButton filled without contrast).
- When a component needs a concrete color string for icons, SVG props, React Native props, gradients, or other non-token props, read it with `const theme = useTheme()` and call `.get()` (e.g. `theme.foregroundPrimary.get()`). Do not use `.val` for theme colors and do not read element colors from `getTokens().color`.
- Do NOT wrap `useTheme()` values or `getTokens()` lookups in `useMemo`. `useTheme()` is already reactive — it tracks accessed keys and only re-renders when those values change. `getTokens()` returns static config defined once at setup. This also applies to simple conditional derivations (e.g. ternaries picking between two theme values based on a prop). Only use `useMemo` for genuinely expensive computations.
- For animated components, use `react-native-reanimated` (shared values, `useAnimatedStyle`, `withTiming`/`withSequence`/`withRepeat`). Resolve non-color token-derived pixel values via `getTokens()` (e.g. `getTokens().size.$3.val`). Do NOT wrap `.val` in `Number()` or `String()`. For scheduling JS callbacks from worklet animations, use `scheduleOnRN` from `react-native-worklets` (NOT the deprecated `runOnJS`). Note: `scheduleOnRN(fn, ...args)` calls directly (returns void), unlike the old `runOnJS(fn)(...args)` curried pattern.
- For image filters (grayscale, color transforms), use `react-native-svg` SVG filters (`Filter`, `FeColorMatrix`, etc.) instead of third-party native filter libraries — they work with Fabric/New Architecture out of the box. Render images via SVG `<Image>` with `preserveAspectRatio="xMidYMid slice"` for cover-like behavior.
- For icon props, pass the component type (`React.ComponentType<IconProps>`) not a rendered element. The consuming component destructures with a capital alias (`icon: Icon`) and renders `<Icon color={...} size={...} />` directly. Never use `React.cloneElement` for color injection.

## Gotchas

- Native-only package. Do NOT add `@tamagui/react-native-media-driver` or media queries.
- Do NOT copy from the existing mobile app tamagui config — the new design system is separate.
- Font files are bundled in `src/assets/fonts/` and exported as `vexlFonts`. Consuming apps must call `useFonts(vexlFonts)` from expo-font before rendering. Font configs in `src/config/fonts.ts` include `face` mappings (body weight 600 → `TTSatoshi600`, heading weight 400 → `PPMonument400`, heading weight 700 → `PPMonument700`).
- Always re-export the tamagui config from `src/config/tamagui.config.ts` so consuming apps can reference it in their Babel plugin.
- Use `React.JSX.Element` return type (not bare `JSX.Element`) — the generic-esm tsconfig doesn't provide the global JSX namespace.
- After changes, run `yarn workspace @vexl-next/ui typecheck` to verify.
