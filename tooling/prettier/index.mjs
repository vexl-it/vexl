/** @typedef  {import("prettier").Config} PrettierConfig */

/** @type { PrettierConfig } */
const config = {
  trailingComma: 'es5',
  singleQuote: true,
  bracketSpacing: false,
  quoteProps: 'preserve',
  semi: false,
}

export default config
