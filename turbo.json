{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "clean": {},
    "lint": {},
    "build-watch": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build-watch"]
    }
  }
}
